#pragma once

#include "Aweasome.hpp"

template<typename T>
void _swap(T& first, T& second);

template<typename T>
const T& _min(const T& first, const T& second);

template<typename T>
const T& _max(const T& first, const T& second);

#include "whatever.tpp"
